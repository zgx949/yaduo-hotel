# 原python检测函数-新客弹窗告警

```python
def is_warning(hotel_data) -> bool:
    """
    检查酒店是否有弹窗

    :param hotel_name:
    :return:
    """

    if not isOnline():
        raise Exception("亚朵token已过期")
    else:
        headers = {
            'Accept': 'application/json, text/plain, */*',
            'Accept-Language': 'zh-CN,zh;q=0.9,en;q=0.8,en-GB;q=0.7,en-US;q=0.6',
            'Connection': 'keep-alive',
            'Content-Type': 'application/json',
            'Origin': 'https://wechat.yaduo.com',
            'Referer': 'https://wechat.yaduo.com/',
            'Sec-Fetch-Dest': 'empty',
            'Sec-Fetch-Mode': 'cors',
            'Sec-Fetch-Site': 'same-site',
            'User-Agent': 'Mozilla/5.0 (Windows NT 10.0; Win64; x64) AppleWebKit/537.36 (KHTML, like Gecko) Chrome/128.0.0.0 Safari/537.36 Edg/128.0.0.0',
            'sec-ch-ua': '"Chromium";v="128", "Not;A=Brand";v="24", "Microsoft Edge";v="128"',
            'sec-ch-ua-mobile': '?0',
            'sec-ch-ua-platform': '"Windows"',
            'Proxy-Authorization': get_xiongmao_header()
        }
        params = {
            'token': token,
            'platType': '5',
            'appVer': '3.31.0',
            'channelId': '300001',
            'activitySource': '',
            'activityId': '',
            'activeId': '',
        }
        json_data = {
            'docType': 1,
            'realName': '刘三',  # 姓名随意
            'idCardNumber': IdNumber.generate_id(random.randint(0, 1)),  # 随机生成合法身份证号
            'chainId': hotel_data[0],
        }
        response = requests.post(
            'https://miniapp.yaduo.com/atourlife/chain/newGuestIdentityCheck',
            params=params,
            headers=headers,
            json=json_data, verify=False
        )
        response = response.json()
        if not response['result']:
            return False
        else:
            path = os.path.join(base_dir, 'appendix', 'warning.txt')
            with open(path, 'a+', encoding='utf-8') as f:
                f.seek(0)
                hotels = f.read().splitlines()
                best_match = process.extractOne(hotel_data[1], hotels, scorer=fuzz.ratio)
                if best_match and best_match[1] < 100:
                    f.write(hotel_data[1] + '\n')
            return True
```